import styled from "styled-components";
import {motion} from 'framer-motion';

const LoginFrameFormStyled = styled(motion.form)`
    display: flex;
    flex-flow: column nowrap;
    justify-content: center;
    align-items: center;
`

export default LoginFrameFormStyled;